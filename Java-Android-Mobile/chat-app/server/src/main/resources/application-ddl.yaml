spring:
  application:
    name: ddl
  main:
    web-application-type: none
  jpa:
    properties:
      jakarta:
        persistence:
          create-source: metadata
          scripts:
            action: create
            create-target: docs/sql/ddl-server.sql